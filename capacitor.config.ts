import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ir.baharnaranj.calendar',
  appName: 'تقویم شیشه‌ای بهارنارنج',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
