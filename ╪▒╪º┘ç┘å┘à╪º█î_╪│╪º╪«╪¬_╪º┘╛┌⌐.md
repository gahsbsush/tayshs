# راهنمای گرفتن فایل APK از گیت‌هاب (بدون نیاز به اندروید استودیو)

این پروژه با Capacitor به یک پروژه‌ی اندروید native تبدیل شده و یک ورک‌فلوی
GitHub Actions (فایل `.github/workflows/build-apk.yml`) به آن اضافه شده که
به‌صورت خودکار پروژه را می‌سازد و فایل APK را تحویل می‌دهد.

## مراحل

1. **ساخت ریپازیتوری در گیت‌هاب**
   - وارد GitHub شوید → دکمه `New repository` → یک نام بدهید (مثلاً
     `baharnaranj-calendar`) → `Create repository`.

2. **آپلود این پوشه**
   - همه‌ی فایل‌های داخل این زیپ (به همراه پوشه‌ی مخفی `.github`) را در
     ریشه‌ی ریپازیتوری آپلود/پوش کنید. اگر با مرورگر آپلود می‌کنید، حتماً
     مطمئن شوید پوشه‌ی `.github` هم منتقل شده (در بعضی آپلودرهای درگ‌اند‌دراپ
     فایل/پوشه‌های نقطه‌دار مخفی می‌مانند؛ روش git از خط‌فرمان مطمئن‌تر است):

     ```bash
     cd پوشه-پروژه
     git init
     git add .
     git commit -m "اولین نسخه"
     git branch -M main
     git remote add origin <آدرس-ریپازیتوری-شما>
     git push -u origin main
     ```

3. **اجرای خودکار بیلد**
   - به محض پوش شدن روی شاخه‌ی `main`، به تب **Actions** در صفحه‌ی
     ریپازیتوری خود بروید. یک اجرا با نام «Build Android APK» شروع می‌شود
     (چند دقیقه طول می‌کشد).
   - اگر خودش شروع نشد، از تب Actions روی ورک‌فلوی «Build Android APK»
     کلیک کنید و دکمه‌ی `Run workflow` را بزنید.

4. **دانلود فایل APK**
   - وقتی اجرا با علامت سبز ✅ تمام شد، روی همان اجرا کلیک کنید.
   - در پایین صفحه، بخش **Artifacts** یک فایل با نام
     `baharnaranj-calendar-apk` می‌بینید؛ آن را دانلود کنید. داخلش
     `app-debug.apk` است که مستقیم روی گوشی اندروید قابل نصب است (باید
     نصب از منابع ناشناس را برای مرورگر/فایل‌منیجر فعال کنید).

## نکات مهم

- این نسخه‌ی APK از نوع **debug** است، یعنی برای تست و نصب شخصی مناسب است.
  اگر می‌خواهید اپ را در **Google Play** منتشر کنید، باید نسخه‌ی
  **release** بسازید و آن را با یک کلید امضا (keystore) امضا کنید؛ در آن
  صورت بگویید تا مراحل ساخت کلید و اضافه‌کردن آن به ورک‌فلو (به‌صورت امن با
  GitHub Secrets) را هم برایتان اضافه کنم.
- بخش‌های مربوط به Firebase (`firebase-applet-config.json`) و کلید
  Gemini (`GEMINI_API_KEY` در `.env.example`) مربوط به سرویس‌های ابری
  هستند؛ اگر این قابلیت‌ها را در اپ استفاده نمی‌کنید نیازی به تنظیم آن‌ها
  نیست و اپ همچنان بدون خطا بیلد و اجرا می‌شود.
- نام بسته‌ی اپ (Application ID) روی `ir.baharnaranj.calendar` تنظیم شده
  (در فایل `capacitor.config.ts` و `android/app/build.gradle`)؛ اگر
  می‌خواهید آن را عوض کنید بگویید.
- برای تغییر آیکون و اسپلش‌اسکرین اپ، بگویید تا فایل‌های لازم را هم آماده
  کنم (الان از آیکون پیش‌فرض Capacitor استفاده می‌شود).
