# راهنمای راه‌اندازی سیستم لایسنس (Cloudflare — روش داشبورد وب)

این پروژه یک سیستم لایسنس ساده دارد که با آن هر کد فقط روی یک گوشی قابل فعال‌سازی است.
تمام مراحل زیر فقط با کلیک توی سایت Cloudflare انجام می‌شه — نیازی به نصب هیچ ابزاری
(مثل wrangler یا ترمینال) نیست.

دو بخش دارد:

1. `cf-license-worker/` → بک‌اند روی Cloudflare (صدور و اعتبارسنجی کدها)
2. `src/lib/license.ts` و `src/components/LicenseGate.tsx` → صفحه‌ی قفل داخل اپ اندروید

---

## مرحله ۱: ساخت اکانت Cloudflare

اگه اکانت نداری، برو **cloudflare.com** و یک اکانت رایگان بساز (فقط ایمیل و رمز عبور، نیاز به کارت بانکی نیست).

## مرحله ۲: ساخت Worker

1. از منوی سمت چپ داشبورد Cloudflare برو روی **Compute** (یا Workers & Pages).
2. روی **Create Application** بعد **Create Worker** بزن.
3. یه اسم بهش بده (یا همون اسم پیش‌فرضی که Cloudflare می‌سازه رو نگه دار) و بزن **Deploy** — یک Worker خالی و پیش‌فرض ساخته می‌شه.

## مرحله ۳: جایگزینی کد Worker

1. روی همون Worker که ساختی بزن **Edit Code** (ویرایشگر آنلاین باز می‌شه).
2. هر چی کد پیش‌فرض توش هست رو پاک کن.
3. کل محتوای فایل `cf-license-worker/src/index.js` (همینجا در پروژه) رو کپی کن و در ویرایشگر پیست کن.
4. بزن **Save and Deploy**.

## مرحله ۴: ساخت پایگاه‌داده KV

1. از منوی سمت چپ برو روی **Storage & Databases** بعد **KV**.
2. بزن **Create Namespace**، اسمش رو بذار `LICENSES` و بساز.

## مرحله ۵: وصل کردن KV به Worker

1. برگرد به صفحه‌ی Worker خودت، برو تب **Bindings**.
2. بزن **Add binding**، نوعش رو **KV namespace** انتخاب کن.
3. توی **Variable name** دقیقاً بنویس `LICENSES` (این اسم باید عیناً با چیزی که در کد Worker به‌عنوان `env.LICENSES` استفاده شده یکی باشه).
4. از لیست namespace، همونی که در مرحله ۴ ساختی رو انتخاب کن و ذخیره کن.

## مرحله ۶: ساخت رمز ادمین

1. توی همون صفحه‌ی Worker برو تب **Settings**.
2. بخش **Variables and Secrets** رو پیدا کن، بزن **Add variable**.
3. در Key بنویس: `ADMIN_SECRET`
4. در Value یک رمز قوی و دلخواه بذار (فقط حروف و عدد انگلیسی، بدون فاصله یا کاراکتر خاص، حداقل ۲۰ کاراکتر).
5. اگه گزینه‌ی **Secret** یا سوییچی کنارش بود، فعالش کن (برای امنیت بیشتر).
6. ذخیره کن.

**این رمز رو جایی برای خودت یادداشت کن — بدونش نمی‌تونی کد لایسنس بسازی. توی ویدیوی آموزشی هم نشونش نده.**

## مرحله ۷: گرفتن آدرس Worker

بالای صفحه‌ی Worker، یک آدرس می‌بینی شبیه:
```
https://your-worker-name.your-subdomain.workers.dev
```
این آدرس رو کپی کن — سرور لایسنست الان زنده‌ست. می‌تونی با بازکردنش توی مرورگر تست کنی؛ دیدن پیغام `{"ok":false,"message":"مسیر یافت نشد."}` کاملاً طبیعیه و یعنی Worker داره درست کار می‌کنه (چون مسیر اصلی `/` در کد تعریف نشده، فقط `/activate` و مسیرهای ادمین).

## مرحله ۸: وصل کردن اپ به Worker

فایل `src/lib/license.ts` رو باز کن و این خط رو پیدا کن:
```ts
export const LICENSE_API_BASE_URL = 'https://REPLACE-WITH-YOUR-WORKER-URL.workers.dev';
```
آدرس واقعی مرحله‌ی ۷ رو جایگزینش کن و سیو کن.

## مرحله ۹: ساخت اولین کد لایسنس

فایل `cf-license-worker/generate-license.js` رو باز کن و این دو خط رو با اطلاعات خودت جایگزین کن:
```js
const WORKER_URL = 'https://your-worker-name.your-subdomain.workers.dev';
const ADMIN_SECRET = 'همون رمزی که در مرحله ۶ ساختی';
```
سیو کن، توی همون پوشه یه CMD باز کن و بنویس:
```
node generate-license.js
```
یک کد لایسنس مثل `AB3D-9KLM-7XYZ` روی صفحه چاپ می‌شه. هر بار خواستی کد بیشتر بسازی، عدد `COUNT` رو توی همون فایل عوض کن و دوباره اجرا کن.

## مرحله ۱۰: انتشار روی گیت‌هاب

حالا که آدرس Worker داخل کد جایگزین شده، کل پروژه رو (شامل پوشه‌ی مخفی `.github`) پوش کن گیت‌هاب. تب **Actions** خودش یک APK جدید و کامل با لایسنس فعال می‌سازه؛ از بخش **Artifacts** دانلودش کن.

---

## نکات مهم

- شناسه‌ی دستگاه از پلاگین `@capacitor/device` گرفته می‌شود؛ این شناسه با نصب اپ ساخته می‌شود و تا زمانی که کاربر اپ را حذف نکند ثابت می‌ماند.
- اگه بعد از نصب اپ، پیغام «اتصال به اینترنت برقرار نیست» گرفتی با اینکه اینترنت وصله، اول مطمئن شو در مرحله‌ی ۸ آدرس واقعی Worker رو جایگزین کردی (نه پلیس‌هولدر پیش‌فرض) و نسخه‌ی قدیمی اپ رو قبل از نصب نسخه‌ی جدید حذف (Uninstall) کردی.
- این روش یک قفل عملی و مناسب برای فروش B2B (کسب‌وکارهای کوچک) است، اما جلوی کاربر خیلی فنی که گوشی root کرده یا دیتای اپ را دستکاری کند را صددرصد نمی‌گیرد. برای بیشتر مشتریان کافی است.
- اگه این پروژه رو در اختیار دیگران هم می‌ذاری (مثلاً برای آموزش)، بهتره بگی هرکس باید Worker و KV جدا روی اکانت Cloudflare خودش بسازه و آدرسش رو در `src/lib/license.ts` جایگزین کنه — این‌طوری هرکس سیستم لایسنس مستقل خودش رو داره.
